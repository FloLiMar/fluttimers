import 'package:flutter/widgets.dart';

class SequenceListWidget extends StatelessWidget {
  const SequenceListWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('sequencelist');
  }

}